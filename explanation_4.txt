# Explanation:
# It's very similar to the File Recursion one, which you have to 
# implement a recursive function to reach the deepest node. So the
# time complexity is also O(n^2), since you have n groups, and each
# group has n groups and users.
# And the space compleity is also O(n^2).
